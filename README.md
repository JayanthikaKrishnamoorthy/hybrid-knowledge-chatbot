# hybrid-knowledge-chatbot
Python chatbot that uses a JSON knowledge base with fuzzy matching to answer queries. If no match is found, it scrapes web data using BeautifulSoup and requests. It also learns new Q&amp;A pairs dynamically, making it a self-improving hybrid knowledge assistant.
